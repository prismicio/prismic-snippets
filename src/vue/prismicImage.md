---
name: "Prismic Image"
description: "Yields a ready-to-use `<PrismicImage />` component"
scope: ["vue-html", "html"]
prefix: "prismicImage"
---

```html
<PrismicImage :field="${1:doc.data.field}" />
```
